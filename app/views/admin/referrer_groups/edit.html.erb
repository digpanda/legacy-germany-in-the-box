<div class="details">
  <div class="row details__block">
    <h3>Edit referrer token <%= @referrer_group.name %></h3>
    <hr>
    <%= form_for [:admin, @referrer_group] do |form| %>
      <%= render partial: "admin/referrer_groups/form", :locals => { :form => form } %>
      <%= form.submit 'Edit referrer token", {:class => "btn btn-primary"} %>
    <% end %>
  </div>
</div>
