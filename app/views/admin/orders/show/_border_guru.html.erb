<h3>BorderGuru</h3>

  BorderGuru Order Id : <%= @order.border_guru_order_id %><br />

  <% if @order.border_guru_link_tracking %>
    <%= link_to "BorderGuru Link Tracking", @order.border_guru_link_tracking, :target => '_blank', :class => "btn btn-primary" %>
  <% else %>
    BorderGuru Link Tracking : <%= editable_field(@order, admin_order_path(@order), :border_guru_link_tracking, :text_field) %>
  <% end %>

  <br />

  <% if @order.border_guru_shipment_id %>
  <!-- TODO: shouldn't we use the same way than when we want the track id from the admin ? I think so. This is then to refactor -->
    <%= link_to "BorderGuru Label", shared_order_label_path(@order), class: "btn btn-primary" %>
  <% else %>
    BorderGuru Shipment ID : <%= editable_field(@order, admin_order_path(@order), :border_guru_shipment_id, :text_field) %>
  <% end %>

  <br />
  <%= link_to "Reset Order from BorderGuru Api", admin_order_reset_border_guru_order_path(@order), method: :patch, class: "btn btn-primary" %>
