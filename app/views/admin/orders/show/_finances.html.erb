  <div class="col-md-12">
    <div class="row">
        <h3>Finances</h3>
      <hr>
    </div>
    <div class="row +overflow">

      Bill ID : <%= editable_field(@order, :bill_id, :text_field, admin_order_path) %>
      <br />

      Total Paid : <%= @order.total_paid_in_yuan %> (<%= @order.total_paid_in_euro %>)<br />
      Total Price : <%= @order.decorate.total_sum_in_yuan %> (<%= @order.decorate.total_sum_in_euro %>)<br />

      <%= render partial: "admin/order_payments/table", :locals => { :order_payments => @order.order_payments.order(c_at: :desc) } %>

    </div>
  </div>
