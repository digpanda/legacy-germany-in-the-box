<nav>
<% if @categories.empty? %>
  <ul class="col-md-2">
  </ul>
<% else %>
  <ul>
    <% @categories.each do |category| %>
    <li class="col-md-2">
      <a href="<%= guest_category_path(category) %>"><%= category.name %></a>
    </li>
    <% end %>
  </ul>
<% end %>
</nav>
