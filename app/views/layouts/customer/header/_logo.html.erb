<!-- Logo -->
<div class="+centered">
  <% if @freeze_header %>
    <a href="<%= identity_solver.origin_url %>">
      <img src="/images/logo-half.png">
    </a>
  <% else %>
    <a href="<%= root_path %>">
      <img src="/images/logo-half.png">
    </a>
  <% end %>
</div>
