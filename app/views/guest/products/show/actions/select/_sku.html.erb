<!-- Sku for product select -->
<div class="product-select">

<span>
  <%= I18n.t(:variant_option, scope: :cart) %>
</span>

<%= select_tag 'sku[option_ids]', options_for_select( get_options_for_select(product), product.decorate.featured_sku.option_ids.join(',') ), :class => 'form-control product-select__dropdown', :id => 'option_ids', :product_id => product.id %>

</div>

<div class="clearfix"></div
