<%= render "guest/home/show/landing_banner" %>

<%= render "guest/package_sets/index/navigation" %>

<% @package_sets.each_with_index do |package_set, index| %>

  <%= render "guest/package_sets/index/block", :options => { :reversed => (index % 2 != 0) }, :package_set => @package_sets.first %>

<% end %>
