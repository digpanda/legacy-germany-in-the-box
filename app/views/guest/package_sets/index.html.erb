<%= render partial: "guest/package_sets/index/navigation", :locals => { package_sets: @package_sets, category: @category, back: guest_package_sets_categories_path  } %>

<%= render "guest/package_sets/package_set_banner" %>

<div class="+small-spacer"></div>

<%= render partial: "guest/package_sets/index/brand_filters", locals: { current_brand: @brand, current_category: @category, brand_filters: @brand_filters} %>

<div class="row">

  <!-- The positioning is impossible to do before we get the actual array -->
  <% @package_sets.sort_by(&:position).each_with_index do |package_set, index| %>
    <%= render "guest/package_sets/index/block", :options => { :reversed => (index % 2 != 0) }, :package_set => package_set %>
  <% end %>

</div>

<div class="+small-spacer"></div>
