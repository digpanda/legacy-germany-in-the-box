<%= render "guest/package_sets/package_set_banner" %>

<div class="+small-spacer"></div>

<%= render partial: "guest/package_sets/brand_filters", locals: { current_brand: @brand, current_category: @category, brand_filters: @brand_filters} %>
<%= render partial: "guest/package_sets/services_brand_filters", locals: { current_brand: @brand, services_brand_filters: @services_brand_filters} %>

<div class="+small-spacer"></div>

<% Category.with_package_sets.order_by(position: :asc).each_with_index do |category, index| %>
  <%= render "guest/package_sets/categories/block", :options => { :reversed => (index % 2 != 0) }, :category => category %>
<% end %>

<a class="big-button --green" href="<%= guest_package_sets_path(category_id: 'all') %>"><%= I18n.t('button.see_all_products') %></a>
<div class="+spacer"></div>

<!-- Services -->
<% if current_user&.tester? %>
  <%= render "guest/package_sets/categories/services" %>
  <div class="+spacer"></div>
<% end %>
