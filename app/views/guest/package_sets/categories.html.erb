<%= render "guest/package_sets/package_set_banner" %>

<div class="+small-spacer"></div>

<p class="categories-block__brands +centered">
  <% Brand.with_package_sets.used_as_filters.each do |brand| %>
    <a href="<%= guest_package_sets_path(brand_id: brand.id) %>"><%= brand.name %></a> |
  <% end %>
</p>

<div class="+small-spacer"></div>

<% Category.with_package_sets.each_with_index do |category, index| %>
  <%= render "guest/package_sets/categories/block", :options => { :reversed => (index % 2 != 0) }, :category => category %>
<% end %>

  <a class="big-button --green" href="<%= guest_package_sets_path(category_slug: 'all') %>"><%= I18n.t('button.see_all_products') %></a>
  <div class="+spacer"></div>
