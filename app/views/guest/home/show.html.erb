<!-- NOTE : this should be refactored and split in a clean way -->
<% if potential_customer? %>

  <!-- Landing page cover -->
  <%= render partial: 'shared/partials/cover', locals: {
      cover: Setting.instance.landing_page_cover,
      banner: @banner,
      sticker: { url: solve_incubator_url, image: '/images/stickers/incubator.jpg' }
      } %>


  <% @shops.each do |shop| %>
    <!-- < cache shop do > -->
      <%= render partial: "guest/shops/block", :locals => { :shop => shop } %>
    <!-- < end > -->
  <% end %>

<% elsif potential_shopkeeper? %>

  <%= render "guest/home/show/marketplace" %>

<% end %>
