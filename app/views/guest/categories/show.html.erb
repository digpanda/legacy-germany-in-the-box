<div class="row">
  <div class="col-md-12">
    <div class="banner lazy" data-original="<%= '/images/categories/category-banner-sample.jpg' %>" style="background-image:url('/images/grey.gif');">
      <h1><%= @category.name %></h1>
    </div>
  </div>
</div>

<%= render "guest/categories/show/navigation" %>

<% @shops.each do |shop| %>

  <%= render partial: "guest/shops/block", :locals => { :shop => shop } %>

<% end %>
