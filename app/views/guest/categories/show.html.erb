<%= render partial: 'shared/partials/cover', locals: { cover: @category.cover, banner: @category.banner, share: current_user&.referrer } %>

<% @shops.each do |shop| %>

  <%= render partial: "guest/shops/block", :locals => { :shop => shop } %>

<% end %>
