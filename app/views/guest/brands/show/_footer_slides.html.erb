<% if brand.footer_slides.count > 0 %>
<div id="slider-vue">

  <swiper :options="swiperOption" class="slider">

    <% brand.footer_slides.each do |image| %>
    <swiper-slide>
      <a href="#">
        <img src="<%= image.file %>" />
      </a>
    </swiper-slide>
    <% end %>

    <div class="swiper-pagination" slot="pagination"></div>
  </swiper>

</div>
<% end %>
