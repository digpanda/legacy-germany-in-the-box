<% if products.size > 0 %>
<div class="main-table">
  <table>
    <thead>
      <tr>
        <th>
          <label>
            <%= I18n.t('edit_product_detail.image') %>
          </label>
        </th>
        <th>
          <label>
            <%= I18n.t('edit_product.name') %>
          </label>
        </th>
        <th>
          <label>
            <%= I18n.t('edit_product.brand') %>
          </label>
        </th>
        <th>
          <label>
            <%= I18n.t('edit_product_detail.quantity') %>
          </label>
        </th>
        <th>
          <label>
            <%= I18n.t('edit_product.operations') %>
          </label>
        </th>
      </tr>
    </thead>
    <tbody>
      <% products.each do |product| %>
        <tr>
          <td>
            <%= image_tag product.decorate.first_sku_image, :class => 'icon-img' %>
          </td>
          <td>
            <%= product.name %>
          </td>
          <td>
            <%= product.brand&.name %>
          </td>
          <td>
            <%= I18n.t('edit_product.quantity_info', total_skus_quantities: product.total_skus_quantities, product_options_size: product.options.size) %>
          </td>
          <td align="left">
            <%= render partial: 'shopkeeper/products/table/action', :locals => { product: product } %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
<% else %>
<div class="col-md-6 col-md-push-3 col-md-pull-3 +white-box +centered">
  <h4>
   <%= I18n.t('result.not_found') %>
 </h4>
</div>
<% end %>
