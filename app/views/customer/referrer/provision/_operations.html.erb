<% unless operations.count == 0 %>
<div class="referrer__operations">

  <!-- OPERATIONS LIST -->
  <div class="referrer__title">
    <%= I18n.t('referrer.operations') %>
  </div>

  <div class="referrer__list">
    <table>
      <thead>
        <th>
          <%= I18n.t('referrer.transfer') %>
        </th>
        <th class="referrer__large">
          <%= I18n.t('referrer.description') %>
        </th>
        <th>
          <%= I18n.t('referrer.date') %>
        </th>
      </thead>
      <tbody>

        <% operations.each do |operation| %>
          <tr data-href="#">
            <td>
              <span class="referrer__list --blue --link"><%= operation.amount.in_euro.display %></span>
            </td>
            <td>
              <%= truncate operation.desc, length: 25 %>
            </td>
            <td>
              <%= operation.c_at.strftime("%Y-%m-%d") %>
            </td>
          </tr>
        <% end %>

      </tbody>
    </table>
  </div>

</div>

<div class="+spacer"></div>
<% end %>
