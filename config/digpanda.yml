defaults: &defaults
  products_warning: 3 # "only X left" system on product page

local:
  <<: *defaults

development:
  <<: *defaults

test:
  <<: *defaults

staging:
  <<: *defaults

preproduction:
  <<: *defaults

production:
  <<: *defaults
